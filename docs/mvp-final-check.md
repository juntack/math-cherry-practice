# MVP仕上げ確認

確認日: 2026-06-27

## 判定

MVPとして必要な主要機能は実装済み。フェーズ5「表示改善」まで完了扱いとし、Web版MVPとしては完成判定とする。

次の主要作業は、新しい学習機能の追加ではなく、Fire HD向けオフラインAPK化に移る。

## MVP完了条件の確認

| 条件 | 状態 | 確認内容 |
| --- | --- | --- |
| たしざん、ひきざん、どちらもを選択できる | 済み | 設定画面の出題モードで選択できる |
| 問題数を選択できる | 済み | 3もん、5もん、10もんを選択できる |
| さくらんぼ、あんざんを選択できる | 済み | やりかたで選択できる |
| ヒントのりょうを選択できる | 済み | おおい、ふつう、すくないを選択できる |
| あんざんではヒントのりょうが無効表示になる | 済み | あんざん選択時にヒントのりょう欄を無効化する |
| テンキー型の数字ボタンのみで回答できる | 済み | 0から19までの答えをテンキーで入力できる |
| 不正解時に答えを表示せず、ヒントを出せる | 済み | 誤答時はヒントだけを表示し、正解は自動表示しない |
| 問題生成、ステップ生成、正誤判定、ヒント生成がUIから独立している | 済み | `src/logic/` と `src/domain/` に分離している |
| 主要ロジックに単体テストがある | 済み | 問題生成、ステップ生成、正誤判定をテストしている |

## 実装済みの補助機能

- 正解時の○印と「ピンポーン」音
- 不正解時の×印と「ブブーッ」音
- 1問完了時のせいかい表示
- 練習途中でトップへ戻る操作
- セッション終了画面
- スマホ実機確認用のLAN表示

## 確認済みコマンド

```sh
npm test
npm run build
```

確認結果:

- `npm test`: 3 files / 13 tests passed
- `npm run build`: success

## 仕上げ確認で修正した点

正解・不正解フィードバックの短い待ち時間中にトップへ戻った場合、古いタイマーが後から状態を更新する可能性があったため、画面遷移や問題リセット時にタイマーを破棄するようにした。

## 表示改善

タブレット・PCでは、練習画面を問題表示側と回答側の2カラムに分けた。さくらんぼ図や10フレームを横いっぱいに広げず、テンキーを右側に固定的に見せることで、縦スクロールをしなくても回答しやすい構成にした。

練習中の正解数表示は削除した。現在の仕様では、不正解でも正解するまで再回答できるため、セッション中の正解数は学習状態を適切に表さない。

## 残リスク

- スマホ表示は開けることを確認済みだが、MVPではタブレット優先とする。
- 効果音はブラウザの自動再生制限や端末設定の影響を受ける可能性がある。
- 暗算モードのヒント文は既存の考え方を使うため、完全な暗算専用文言にはなっていない。
- Fire HDでは、画面サイズ、タッチ操作、音の鳴り方を実機で確認する必要がある。

## 次の作業

- Debug APKをFire HD実機へインストールする
- Fire HD実機でオフライン起動、表示、タッチ、効果音を確認する

ドラッグ操作、引き算方式切り替え、ローカル成績保存、10フレームの詳細アニメーションなどは、MVP後の別フェーズ候補とする。
